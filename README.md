# Naod HD Camera v1.5.3

Final Android build-ready project.

- Naod photo is used as the Pro Settings background and camera avatar.
- Existing camera features are preserved.
- `pubspec.yaml` includes the required image asset declaration; this is required for Flutter to package the photo inside the APK.
- GitHub Actions builds the release APK.
